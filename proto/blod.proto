syntax = "proto3";
package blodBank;

option go_package = "./blodBank";

service SystemService { rpc Ping(NoParam) returns (Status); }

// service DonorService {
//   rpc RegisterDonor(donorInfo) returns (DonorId);
//
//   rpc UpdateDonor(donorInfo) returns (Status);
//
//   rpc GetDonor(donorID) returns (donorInfo);
// }

message NoParam {};

message Status { string message = 1; };

message Samples {
  string id = 1;
  string type = 2;
}

message Type {
  string id = 1;
  string type = 2;
  string name = 3;
}

message Cert {
  string id = 1;
  string name = 2;
  string type = 3;
}

// message Status {
//   string certId = 1;
//   string status = 2;
// }
